// Пример локального конфига. Скопируйте в local-config.js и впишите свой токен
// (его передают лично — см. README). Файлы local-config.js не коммитятся.
window.__EXT_LOCAL_CONFIG__ = {
  token: "",
  // baseUrl и model можно переопределить здесь же, если дефолты из config.js не подходят:
  // baseUrl: "http://localhost:8080",
  // model: "qwen/qwen3.8-27b",
};